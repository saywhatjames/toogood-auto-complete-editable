import { SuggestionsDirective } from './suggestions.directive';

describe('SuggestionsDirective', () => {
  it('should create an instance', () => {
    const directive = new SuggestionsDirective();
    expect(directive).toBeTruthy();
  });
});
